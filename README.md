# DENOS — Extended Starter

This update adds:
- Upload page (camera + file + text) with language selection
- Study page with a simple SRS algorithm
- Server endpoints for news ingestion and YouTube transcript -> flashcards
- Push subscription endpoint placeholder and Firebase messaging service worker
- Firestore security rules example

Deploy notes:
- You must configure Firebase client and Admin SDK credentials (service account) for server push features
- Add OpenAI and Google credentials to environment
